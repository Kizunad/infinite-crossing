# ==============================================================================
# AI Model Configuration
# ==============================================================================
# Common API Key (supports OpenAI-compatible endpoints)
GEMINI_API_KEY=your_api_key_here
# OPENAI_API_KEY=your_openai_key_here

# Optional: Custom base URL for OpenAI-compatible endpoints (e.g., local LLM server)
# BASE_URL=https://api.openai.com/v1

# Default model (used as fallback if specific agent model is not set)
LLM_MODEL=gemini-2.0-flash

# ==============================================================================
# Multi-Model Configuration (Optional)
# Configure different models for different agents based on task complexity.
# If not set, the agent will fall back to LLM_MODEL.
# ==============================================================================

# Heavy Task: Core rule validation and state changes. Requires precise reasoning.
LLM_MODEL_JUDGE=gemini-2.0-pro
LLM_MODEL_WORLD=gemini-2.0-flash
LLM_MODEL_QUEST=gemini-2.0-flash

# Light Tasks: Primarily text generation and formatting.
LLM_MODEL_NARRATOR=gemini-2.0-flash-lite
LLM_MODEL_ARCHIVIST=gemini-2.0-flash-lite

# Light Task: History compression for context management.
LLM_MODEL_COMPRESSOR=gemini-2.0-flash-lite
LLM_MODEL_GENERATOR=gemini-2.0-flash

# Light Task: Environment state extraction from narrative.
LLM_MODEL_ENVSTATE=gemini-2.0-flash-lite

# ==============================================================================
# Supabase Configuration
# ==============================================================================
# Get these from your Supabase project settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
